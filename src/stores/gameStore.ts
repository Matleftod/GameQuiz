import { defineStore } from 'pinia';

export const useGameStore = defineStore('game', {
  // État
  state: () => ({
    // Votre état ici
  }),

  // Getters
  getters: {
    // Vos getters ici
  },

  // Actions
  actions: {
    // Vos actions ici
  },
});