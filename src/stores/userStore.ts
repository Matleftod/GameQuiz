import { defineStore } from "pinia";

export const useUserStore = defineStore("user", {
  // État
  state: () => ({
    // Votre état ici
  }),

  // Getters
  getters: {
    // Vos getters ici
  },

  // Actions
  actions: {
    // Vos actions ici
  },
});
